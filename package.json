{
  "name": "javy-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "example": "examples"
  },
  "scripts": {
    "build:ts": "npx esbuild src/index.ts --bundle --platform=node --format=esm --outfile=./build/index.js",
    "build:wasm": "/home/udit/Work/experiments/javy-project/javy/target/release/javy compile build/index.js -o build/index.wasm",
    "build": "npm run build:ts && npm run build:wasm",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.19.8"
  },
  "dependencies": {
    "ethers": "^6.9.0"
  }
}
