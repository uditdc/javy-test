{
  "name": "browser-runtime",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "npx esbuild src/index.ts --bundle --outdir=dist",
    "build:worker": "npx esbuild src/wasm.ts --bundle --outdir=dist/workers",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@bjorn3/browser_wasi_shim": "^0.2.17"
  },
  "devDependencies": {
    "esbuild": "^0.19.10"
  }
}
